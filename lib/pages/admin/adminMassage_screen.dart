import 'package:flutter/material.dart';

class AdminMassageScreen extends StatelessWidget {
  const AdminMassageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('mass')],
    );
  }
}
